{
  "name": "responseTime",
  "version": "0.1.0",
  "description": "check messaging response times",
  "main": "checkResponse.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "optimist": "*",
    "request": "*",
    "async": "*"
  }

}
